#include "sio/panic.h"

#include <gtest/gtest.h>

namespace sio {

TEST(Panic, Basic) {
    //Error err = Error::OutOfMemory;
    //if (err != Error::OK) {
    //  SIO_PANIC(err);
    //}
}

} // namespace sio