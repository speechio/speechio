resampler:
    sample_rate: 16000

fbank_feature_extractor:
    num_mel_bins: 80
    dither: 0.0

tokenizer: 
    vocab_path: tokenizer.vocab
    model_path: tokenizer.model

model_name: wenet
model_hparam: wenet.yaml

test_set:
    subsets:
    -   id: AISHELL-1-TEST
        max_num_samples: -1
